var me = "Alexandra"
var mom = "Angelica"

if(me.length>mom.length){
    console.log(` ${me} is longer than ${mom} by ${me.length-mom.length} characters`);
}else{
    console.log(`${mom} is longer than ${me} by ${mom.length-me.length} characters`);
}